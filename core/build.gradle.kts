plugins {
    id("java-library")
}

dependencies {
    // =====================================================
    // Core는 순수 유틸리티 모듈 - domain, infra에 의존하지 않음
    // =====================================================

    // 현재 core에 남은 코드가 없으므로 의존성 없음
}
